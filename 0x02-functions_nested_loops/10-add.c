#include "main.h"
/**
 * add - a fxn that adds two intergers and runs the result
 * @one: First number input
 * @two: second number
 * Return: one + two
 */
int add(int one, int two)
{
	return (one + two);
}
